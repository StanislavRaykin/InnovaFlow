@inherits LayoutComponentBase

@* Centres a single auth card on the page. Replaces the body-level utility
   classes the original standalone HTML documents carried. Page-specific
   colour/font classes stay on each page's own root element. *@
<div class="min-h-screen flex items-center justify-center p-4">
    @Body
</div>
